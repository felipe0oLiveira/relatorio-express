import { NextResponse } from 'next/server';
import type { NextRequest } from 'next/server';

export function middleware(request: NextRequest) {
  // Desabilitar completamente o middleware para evitar redirecionamentos
  return NextResponse.next();
}

export const config = {
  matcher: [
    // Comentar todas as rotas para desabilitar o middleware
    // '/dashboard/:path*',
  ],
}; 